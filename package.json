{
  "name": "daki-playground",
  "version": "0.1.0",
  "private": true,
  "description": "Public, sandboxed demo of the daki agent pipeline: file an issue, watch it become a reviewed, auto-merged PR that repaints a tiny web page.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs docs/scene.json",
    "test": "node scripts/test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "ajv": "^8.17.1"
  }
}
